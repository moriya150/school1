BEGIN;

UPDATE
	NAME_TABLE
SET
    RANK = 'C'
WHERE
    NAME_ID IN
(
SELECT
	NAME_ID
FROM
	RESULT_TABLE
WHERE (select AVG(SCORE) from RESULT_TABLE)<10
)
;

SELECT
	*
FROM
	RESULT_TABLE
WHERE
    NAME_ID IN
(
SELECT
	NAME_ID
FROM
	NAME_TABLE
WHERE
    RANK = 'A' OR RANK = 'B'
);

COMMIT;